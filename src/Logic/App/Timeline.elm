module Logic.App.Timeline exposing (..)

{-
   node for each pattern
   hermes - node for each pattern in list
   thoth - node for each pattern in list per iota in other list
-}
{-
   node for every stack state
   associated pattern
-}
{-
   mind
   {
       mind
       mind
   }
   hermes

   (mind) ({) (mind) (mind) (}) (mind) (mind) (hermes)

   pattern array -- get the patterns
   figure out what list hermes/thoth executes
-}
{-
   store every stack state into array
   associate a pattern with each state
   -- might require each pattern to have an id?
-}